{
  "name": "clawguard",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clawguard": "node packages/cli/dist/cli.js",
    "dev:scan-clawhub-bundles": "pnpm -r build && node scripts/scan-clawhub-bundles.mjs --limit 200 --out /tmp/clawhub-bundles-200.jsonl"
  },
  "devDependencies": {
    "@types/node": "^22.19.7",
    "typescript": "^5.9.3"
  }
}
