{
  "name": "@clawguard/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "clawguard": "./dist/cli.js"
  },
  "dependencies": {
    "@clawguard/core": "workspace:*"
  },
  "scripts": {
    "build": "node -e \"import('node:fs').then(fs=>fs.rmSync('dist',{recursive:true,force:true}))\" && tsc -p tsconfig.json",
    "test": "pnpm run build && node --test dist/test",
    "lint": "echo \"lint: skip\""
  }
}
